#! /bin/sh

# Halt on error
set -e

# Remove our configuration
[ -L /etc/issue.net ] && rm -fv /etc/issue.net
dpkg-divert --remove --package mylinux-iut --rename /etc/issue.net.backup --divert /etc/issue.net
/usr/sbin/mylinux-update-banner
